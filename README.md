# HERKULES 1934 English



The GitHub tool can write files after a first commit exists, but it cannot create the very first commit in an empty repository. That’s why it keeps asking for a sha.

Create the initial README in the GitHub web UI, then I’ll take over immediately.

